# ============================================================
# 서버 설정
# ============================================================

# ── DB 접속 정보 (AWS RDS 받으면 여기만 수정) ──────────────
DB_CONFIG = {
    "host":       "localhost",        # AWS RDS 엔드포인트로 변경
    "port":       3306,
    "user":       "root",             # DB 계정
    "password":   "1234",          # DB 비밀번호
    "db":         "parking_db",       # DB 이름
    "charset":    "utf8mb4",
    "autocommit": False,
}

# ── 기록 자동 삭제 시간 ────────────────────────────────────
# 테스트용 : 10분
# 1시간    : 60
# 하루     : 60 * 24       = 1440
# 일주일   : 60 * 24 * 7   = 10080
HISTORY_DELETE_MINUTES = 10

# ── 번호판 유사도 매칭 임계값 ──────────────────────────────
# 숫자가 낮을수록 엄격 (1~2 권장)
# 1 : 글자 1개 차이까지 보정
# 2 : 글자 2개 차이까지 보정
PLATE_MATCH_THRESHOLD = 2